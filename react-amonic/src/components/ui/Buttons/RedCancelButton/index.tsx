import {styled} from "@mui/system";

export const RedCancelButton = styled('button')({
    backgroundColor: 'red',
    border: '0',
    borderRadius: '6px',
    color: 'white',
    padding: '10px 15px',
    fontWeight: 'bold',
})